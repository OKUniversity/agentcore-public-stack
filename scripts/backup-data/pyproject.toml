[project]
name = "agentcore-backup-data"
version = "1.0.0"
description = "Pre-migration backup tool for AgentCore Public Stack data and users"
requires-python = ">=3.13"
dependencies = [
    "boto3==1.43.9",
    "botocore==1.43.9",
]

[dependency-groups]
dev = [
    "moto[dynamodb,s3,cognitoidp,ssm]==5.1.4",
    "pytest==9.0.3",
]

[tool.uv]
package = false

# Security: constrain transitive cryptography to patched OpenSSL (GHSA-537c-gmf6-5ccf)
# and to the PKCS#7 Bleichenbacher-oracle fix (CVE-2026-69247), patched in 50.0.0.
constraint-dependencies = ["cryptography>=50.0.1"]
