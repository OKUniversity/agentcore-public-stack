"""Structured clarifying questions the agent can ask mid-turn.

See :mod:`apis.shared.user_questions.models` for the wire contract shared by
the ``ask_user_question`` tool (producer), the streaming layer (transport),
and the session-metadata breadcrumb (reload recovery).
"""
